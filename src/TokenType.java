public enum TokenType {
    TYPE_SHONGKHA,
    TYPE_BAKKO,
    NUMBER,
    STRING,
    IDENTIFIER,
    IF,
    ELSE,
    ASSIGN,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    SEMICOLON,
    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    EOF
}